name: rebuy_flutter_getx
description: A sample Flutter app implementing ReBuy UI with GetX (Signup, Explore, Messages).
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
